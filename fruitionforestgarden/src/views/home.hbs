<div class="hero-content">
    <h1>Fruition Forest Garden</h1>
</div>
{{#if heroImagePath}}
<div class="hero">
    <img src="{{heroImagePath}}" alt="Aerial view of Fruition Forest Garden" class="hero-image lightbox-trigger" data-caption="Aerial view of Fruition Forest Garden">
</div>
{{/if}}

<section class="recent-posts">
    <h2>Recent Posts</h2>
    {{#if posts.length}}
        <div class="post-previews">
            {{#each posts}}
                <sl-card>
                    {{#if this.images.[0]}}
                        <a href="/post/{{this.slug}}" slot="image" class="card-link post-image-link">
                            <img src="{{this.images.[0].medium}}" alt="{{this.title}}" loading="lazy">
                        </a>
                    {{/if}}
                    <a href="/post/{{this.slug}}" class="card-link post-summary-link">
                        <h3>{{this.title}}</h3>
                        {{#if this.excerpt}}
                            <p>{{this.excerpt}}</p>
                        {{else}}
                            <p>{{truncate this.body 150}}</p>
                        {{/if}}
                    </a>
                    <div class="post-meta">
                        <span>Event: {{formatDate this.created_at}}</span>
                        <span>Posted: {{formatDate this.updated_at}}</span>
                        {{> public-byline publicByline=this.publicByline}}
                    </div>
                </sl-card>
            {{/each}}
        </div>

        {{#if pagination.hasMultiplePages}}
            <nav class="pagination" aria-label="Post pages">
                {{#if pagination.previousUrl}}
                    <a href="{{pagination.previousUrl}}" class="page-link">Previous</a>
                {{/if}}
                {{#each pagination.pages}}
                    <a href="{{this.url}}" class="page-link {{#if this.isCurrent}}active{{/if}}" {{#if this.isCurrent}}aria-current="page"{{/if}}>{{this.number}}</a>
                {{/each}}
                {{#if pagination.nextUrl}}
                    <a href="{{pagination.nextUrl}}" class="page-link">Next</a>
                {{/if}}
            </nav>
        {{/if}}
    {{else}}
        <p class="no-posts">No posts yet. Check back soon!</p>
    {{/if}}
</section>
